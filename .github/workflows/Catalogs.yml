name: DBMS Catalog Tests
on: [push, pull_request, repository_dispatch]
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' || github.sha }}
  cancel-in-progress: true
defaults:
  run:
    shell: bash

jobs:
  ducklake-config-tests:
    name: DuckDB Tests with DuckLake + (Postgres/SQLite)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch: ['linux_amd64']
        vcpkg_version: [ '2023.04.15' ]
        include:
          - arch: 'linux_amd64'
            vcpkg_triplet: 'x64-linux'

    env:
      VCPKG_TARGET_TRIPLET: ${{ matrix.vcpkg_triplet }}
      GEN: Ninja
      VCPKG_TOOLCHAIN_PATH: ${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake
      ENABLE_SQLITE_SCANNER: ON
      ENABLE_POSTGRES_SCANNER: ON
      ENABLE_QUACK: ON
      BUILD_EXTENSION_TEST_DEPS: full
      DUCKLAKE_CI: 1

    steps:
    - name: Install required ubuntu packages
      run: |
        sudo apt-get update -y -qq
        sudo apt-get install -y -qq build-essential cmake ninja-build ccache python3 mold

    - name: Setup PostgreSQL for Linux/macOS/Windows
      uses: ikalnytskyi/action-setup-postgres@v7
      with:
        postgres-version: '15'
        username: 'postgres'
        password: 'postgres'
        database: 'postgres'
        port: '5432'
        ssl: true

    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
        submodules: 'true'

    - name: Checkout DuckDB to version
      run: |
        cd duckdb
        git checkout $(cat ../.github/duckdb-version)

    - name: Setup Ccache
      uses: hendrikmuhs/ccache-action@main
      with:
        key: ${{ github.job }}
        save: ${{ github.ref == 'refs/heads/main' || github.repository != 'duckdb/ducklake' }}

    - name: Setup vcpkg
      uses: lukka/run-vcpkg@v11.1
      with:
        vcpkgGitCommitId: cd61e1e26a038e82d6550a3ebbe0fbbfe7da78e3 # Release 2026.06.24

    - name: Build extension
      env:
        GEN: ninja
      run: |
        # enables allow_unsigned_extensions by default for ADBC test below
        pushd duckdb
        patch -p1 < ../test/adbc/allow_unsigned_extensions.patch
        popd
        make release

    - name: Test Quack
      run: |
        python3 scripts/run_quack_tests.py --timeout 300

    - name: Test SQLite
      env:
        DUCKDB_TPCH_ANSWERS_DIR: ${{ github.workspace }}/duckdb/extension/tpch/dbgen/answers
      run: |
        build/release/test/unittest --test-config test/configs/sqlite.json --test-dir ./ "test/sql/*"

    - name: Test Postgres
      env:
        PGSERVICE: postgres
        LOCAL_EXTENSION_REPO: ${{ github.workspace }}/build/release/repository
        DUCKDB_TPCH_ANSWERS_DIR: ${{ github.workspace }}/duckdb/extension/tpch/dbgen/answers
      run: |
        createdb ducklakedb
        build/release/test/unittest --test-config test/configs/postgres.json --test-dir ./ "test/sql/*"

    # see https://github.com/duckdb/duckdb-postgres/issues/577
    - name: Test Postgres ADBC
      env:
        PGSERVICE: postgres
      run: |
        createdb adbc
        mkdir adbc_build
        cd adbc_build
        cmake ../test/adbc/
        make
        ./repro \
          adbc \
          5432 \
          ${{ github.workspace }}/adbc_data \
          ${{ github.workspace }}/build/release/extension/ducklake/ducklake.duckdb_extension \
          ${{ github.workspace }}/build/release/extension/postgres_scanner/postgres_scanner.duckdb_extension \
          90
